| --#{boundary}
| Content-Type: text/plain; charset=UTF-8
| 
| #{posts.length} new messages
| #{uri}
| --#{boundary}
| Content-Type: text/html; charset=UTF-8
| 
html
  head
    title #{posts.length} new messages
  body
    table(border=1)
      tr
        th Folder
        th Subject
        th From
        th To
        th Date
      - for (var post in posts)
        tr
          td #{posts[post].foldername}
          td
            - var a = '/m/'+posts[post].link;
            - var to = posts[post].m_toname;
            - var niceto = to ? to : '';
            a(href: a) #{posts[post].m_subject}
          td #{posts[post].m_fromname}
          td #{posts[post].to}
          td #{posts[post].nicedate}
        tr
          td(colspan:5) 
            pre #{posts[post].wrapped}
    a(href:uri) #{uri}
    br
    small UA Notify Bot says hello!
| 
| --#{boundary}--
